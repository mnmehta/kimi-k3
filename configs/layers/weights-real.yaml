# Real MXFP4 checkpoint on hostPath.
name: weights-real
serve_script: scripts/run-vllm-kimi-k3-recipe.sh
verify_weights: true

env:
  MODEL: /models/Kimi-K3
  MODEL_PATH: /models/Kimi-K3
  # LOAD_FORMAT left unset → vLLM auto
