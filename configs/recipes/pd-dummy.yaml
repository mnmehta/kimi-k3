include:
  - ../layers/base.yaml
  - ../layers/weights-dummy.yaml
  - ../layers/strategy-pd.yaml
  - ../layers/profiler-shallow.yaml

name: pd-dummy
description: Dummy-weight P/D (shallow; optional profiler via env)
deploy_mode: pd

env:
  GPU_MEM_UTIL: "0.90"
  MAX_MODEL_LEN: 1024
  MAX_NUM_BATCHED_TOKENS: 1024
  PROFILE_DIR: /tmp/vllm_profile/pd

pd:
  start_router: true
